require("nvim-tree").setup({
    actions = {
        open_file = { resize_window = false }
    }
--    renderer = { icons = { show = {
--        git = false,
--        file = false,
--        folder = false,
--        folder_arrow = false
--    }}}
})

